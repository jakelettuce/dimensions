export type {
  Bounds,
  ThemeVars,
  AssetInfo,
  SDKResponse,
  WSConnection,
  WidgetManifest,
  WidgetInput,
  WidgetOutput,
  WidgetProp,
  SceneMeta,
  WidgetEntry,
  DimensionMeta,
  Connection,
  PortalRule,
  DimensionsSDK,
} from './types'

// SDK runtime implementation will be built in Phase 3.
// This file will contain the postMessage bridge (~300 lines).
// For now, we export only the types.
