// portal-drag.ts — MINIMAL preload for portal content WCVs.
// Currently a no-op placeholder. Cross-WCV drag uses the download
// confirmation modal ("Save to Dimensions") rather than intercepting
// native drag events — this avoids killing in-portal drag behavior
// and the async download timing problem with OS-level startDrag().

export {}
